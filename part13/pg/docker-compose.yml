services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    volumes:
      # https://github.com/docker-library/docs/blob/master/postgres/README.md#pgdata
      - ./pg_data:/var/lib/postgresql
    ports:
      - 5432:5432
    container_name: blog-app-pg